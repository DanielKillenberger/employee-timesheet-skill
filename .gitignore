# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Build artifacts
dist/
build/
*.zip

# Local employee/payroll data
# Defense in depth: the data directory lives outside every git worktree
# (scripts refuse a data dir inside one), these patterns are the backstop.
data/
employees.json
*.timesheet-data.json
filled-timesheets/
extractions/
templates/

# Generated documents
output/
*.xlsx
*.pdf

# Local agent state
.claude/settings.local.json
